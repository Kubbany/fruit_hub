class ReviewEntity {
  final String name, image, date, reviewDescription;
  final num rating;

  ReviewEntity({
    required this.name,
    required this.image,
    required this.date,
    required this.reviewDescription,
    required this.rating,
  });
}
